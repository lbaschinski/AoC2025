# AoC2025

## Building and execution

- Build and execute via nix:

    ```bash
    nix-build
    result/bin/AoC2025
    ```

- Build and execute via cargo (enable direnv or run `nix-shell` before):

    ```bash
    cargo run
    cargo test
    ```
